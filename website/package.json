{
  "name": "whatsapp-channel-exporter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/prepare-download.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "postbuild": "node scripts/clean-sitemap.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "vite-plugin-sitemap": "^0.8.2"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  }
}
