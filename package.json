{
  "name": "whatsapp-channel-exporter",
  "version": "1.3.1",
  "description": "Local-first Chrome extension for exporting WhatsApp Channel posts from WhatsApp Web into portable archives.",
  "private": true,
  "scripts": {
    "build": "node scripts/generate-icons.js && node scripts/build.js",
    "build:store": "STORE_BUILD=true npm run build",
    "build:chrome": "STORE_BUILD=true STORE_TARGET=chrome ARCHIVE_NAME=wa-channel-exporter-chrome.zip npm run build",
    "build:edge": "STORE_BUILD=true STORE_TARGET=edge ARCHIVE_NAME=wa-channel-exporter-edge.zip npm run build",
    "build:stores": "npm run build:chrome && npm run build:edge",
    "prepare:store-assets": "node scripts/prepare-store-assets.js",
    "test": "node scripts/test-mv3.js && node scripts/test-exports.mjs && node scripts/test-media-download.mjs",
    "test:chrome": "node scripts/test-extension-chrome.mjs dist",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "pdfmake": "^0.3.11",
    "puppeteer": "^25.3.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  },
  "keywords": [
    "whatsapp-channel",
    "whatsapp-web",
    "channel-archive",
    "whatsapp-export",
    "media-archive",
    "chrome-extension",
    "local-first"
  ],
  "license": "AGPL-3.0-or-later",
  "main": "index.js",
  "author": "Karefined Engineering",
  "type": "commonjs"
}
